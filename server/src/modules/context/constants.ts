/**
 * Project Context module (SPEC-01) — shared constants.
 */

/** Default set of folder names discovery scans for `.md` docs, at any depth. */
export const DEFAULT_CONTEXT_FOLDERS = ['specs', 'docs', 'insights'] as const;
