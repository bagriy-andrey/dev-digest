/** View-mode toggle for the results view — Columns vs Tabs render branches
 *  over the SAME fetched `MultiAgentRun` object (AC-33). */
export type ViewMode = "columns" | "tabs";
export const DEFAULT_VIEW_MODE: ViewMode = "columns";
