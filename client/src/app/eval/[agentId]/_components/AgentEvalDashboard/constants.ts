/** Layout/behavior constants for the per-agent eval dashboard. */

/** AC-27 — at most two batches may be selected for comparison at once. */
export const MAX_COMPARE_SELECTION = 2;

/** Batch history table grid: select | version | ran at | pass/total | recall |
 *  precision | citation | cost | status. */
export const BATCH_HISTORY_GRID = "32px 56px 1.3fr 70px 70px 70px 70px 80px 90px";

export const TREND_CHART_WIDTH = 620;
export const TREND_CHART_HEIGHT = 200;
