/** Compact-card caps — the full, untruncated data is always one click away via
 *  "See full map" (BlastTab). Keeps the card readable when a symbol has many
 *  callers/endpoints (e.g. a widely-used shared helper). */
export const MAX_CARD_CALLERS = 6;
export const MAX_CARD_ENDPOINTS = 6;
